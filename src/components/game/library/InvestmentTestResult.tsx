import React from "react";
import styled from "styled-components";

// 투자 성향 테스트 완료 컴포넌트
const InvestmentTestResult = () => {
  return <Wrapper>투자 성향 테스트 결과</Wrapper>;
};

export default InvestmentTestResult;

const Wrapper = styled.div`
  display: flex;
  width: 100%;
`;
