import React from "react";
import styled from "styled-components";

// 서재 (퍼즐 2 ~ 4)
const Library = () => {
  return <Wrapper>서재 컴포넌트</Wrapper>;
};

export default Library;

const Wrapper = styled.div`
  display: flex;
  width: 100%;
`;
